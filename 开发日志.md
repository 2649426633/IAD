# IAD 开发进度与日志

本文件用于持续记录 IAD（Industrial Anomaly Detection）项目的实际开发进度、验证结果和后续事项。

## 日志维护规则

1. 每完成一次功能开发、缺陷修复、架构调整或重要界面优化，都必须在本文件末尾追加一条日志。
2. 日志按完成时间正序排列；只允许在末尾追加，不覆盖、不删除既有记录。历史记录需要纠正时，应追加“更正说明”。
3. 日志必须描述实际完成的内容，不把占位界面、模拟数据或尚未接入的算法写成已完成功能。
4. 每条日志至少包含：开发目标、完成内容、验证结果和遗留事项；有对应提交时可附提交短哈希。
5. 代码与日志应在同一次提交中提交，确保仓库中的开发状态与记录一致。

建议使用以下格式：

```text
### YYYY-MM-DD HH:mm — 开发主题

- 开发目标：
- 完成内容：
- 验证结果：
- 关联提交：
- 遗留事项：
```

## 开发记录

### 2026-08-07 19:31 — 项目初始化

- 开发目标：建立工业瑕疵质检桌面软件的初始工程。
- 完成内容：创建 IAD WinForms 项目和解决方案，确定 .NET Framework 桌面应用基础结构。
- 验证结果：工程可以正常打开并进入后续开发。
- 关联提交：`9202ba1`。
- 遗留事项：业务页面、数据层和算法能力尚未建设。

### 2026-08-07 20:23 — 多页面工业质检平台骨架

- 开发目标：形成覆盖主要业务流程的桌面端页面框架。
- 完成内容：建立工作台、产品定义、数据集标注、瑕疵模板识别、训练与模型、规则与 Recipe、在线检测、结果追溯和系统设置页面；完成主导航和全屏工作台布局。
- 验证结果：各页面能够通过主导航切换和显示。
- 关联提交：`56c56e2` 及其后续页面拆分提交。
- 遗留事项：多数页面仍为界面与接口占位，尚未形成业务闭环。

### 2026-08-08 11:23 — Designer、响应式布局与登录框架

- 开发目标：提高 WinForms 设计器兼容性、页面适配能力和应用入口完整性。
- 完成内容：将页面转换为原生 Designer 控件并分离功能代码；加入统一响应式布局与页面填充管理；修复切换闪动和延迟填充；增加 Windows WinForms 编译检查、登录窗体和会话状态，并完成高 DPI 布局优化。
- 验证结果：主要页面可由 Designer/CodeDom 解析；页面可随主窗口同步填充；登录成功后进入主界面。
- 关联提交：`0fefd32`、`8545c16`、`d391eb6`、`ddd3473`。
- 遗留事项：开发阶段登录流程仍可旁路，完整角色权限与审计尚未实现。

### 2026-08-08 11:45 — SQLite 数据基础设施

- 开发目标：让项目具备离线业务数据持久化能力。
- 完成内容：新增产品、缺陷类别、ROI、Recipe、检测结果和缺陷实例领域模型；建立 Workspace 目录、SQLite 连接工厂、Schema 初始化、Repository、Service 和应用服务注册；统一 x64 运行平台并忽略运行时生产数据。
- 验证结果：应用启动时可初始化 SQLite 和服务容器，Debug x64 构建通过。
- 关联提交：`d0e7a73`。
- 遗留事项：当时只有数据基础设施，除产品定义外的业务页面尚未接入真实数据。

### 2026-08-08 13:53 — 产品定义持久化闭环

- 开发目标：完成第一条真实业务链路。
- 完成内容：产品基本信息、产品定义扩展配置、缺陷类别和 ROI 接入 SQLite；增加产品选择/新建、ROI 管理、缺陷类别扩展字段和配置保存能力。
- 验证结果：产品定义数据可以保存、切换和重新加载，相关 Designer 序列化检查通过。
- 关联提交：`84fb73a`。
- 遗留事项：定位算法、HALCON 图像交互和在线检测链路尚未接入。

### 2026-08-08 16:23 — 页面设计与产品定义基础功能完善

- 开发目标：修复页面设计问题并补齐产品定义的常用操作。
- 完成内容：统一页面布局和视觉规则；修复 WinForms 设计器将页面基类识别为 `System.Void` 的问题；恢复缺失的 `RoiManagerDialog.resx` 构建链路；完善缺陷类别编辑、CSV 导入导出、基准图预览、定位预设、模板配置校验和版本递增；压缩产品基本信息区域的多余空白。
- 验证结果：Debug x64 构建为 0 警告、0 错误；设计时编译与布局测量通过。
- 关联提交：`6d67914`、`fc5e93c`、`250e8c7`、`99f9dcd`。
- 遗留事项：真实定位测试、模板生成和 HALCON 运行时仍待接入。

### 2026-08-08 16:50 — 数据集标注基础功能

- 开发目标：将数据集标注页面从演示数据升级为可使用的基础业务闭环。
- 完成内容：新增数据集图片、标注和版本模型；数据库 Schema 升级至 V4；实现 Dataset Repository/Service；支持按当前产品批量导入并复制图片、真实图片预览、矩形/多边形/画笔标注、橡皮擦删除、Mask 编辑、图层显隐、边界检查、质量与队列统计；标注自动写入 SQLite；发布 `DS-*` 数据集版本时保存图片与标注快照。
- 验证结果：Debug、Release 和 Visual Studio 设计时构建均为 0 警告、0 错误；隔离 Workspace 冒烟测试验证了图片导入、三类标注、删除、显隐、版本递增、版本快照和页面真实数据加载。
- 关联提交：`97b0af7`。
- 遗留事项：HALCON 专用显示控件、SAM/少样本自动标注、像素级 Mask 文件导出、撤销/重做及 COCO/YOLO 格式导入导出尚未实现。

### 2026-08-08 16:53 — 建立持续开发日志制度

- 开发目标：让后续开发进度在仓库内持续、可追溯地记录。
- 完成内容：创建 `开发日志.md`，补录项目初始化至数据集标注闭环的关键阶段，并制定只在文件末尾追加日志的维护规则和统一格式。
- 验证结果：历史进度已根据 Git 提交记录核对，文档结构和 Markdown 格式检查通过。
- 关联提交：本次文档提交。
- 遗留事项：后续每次开发完成时必须继续在本文件末尾追加记录。

### 2026-08-08 17:10 — 产品定义与数据集标注关联闭环

- 开发目标：确保用户在产品定义页面选择并保存产品后，数据集标注页面严格基于该产品加载、标注和保存数据。
- 完成内容：新增可通知页面刷新的当前产品上下文；产品定义页面优先恢复当前选择；移除标注页面在未选择产品时自动取第一条产品的隐式逻辑；标注页面显式显示当前产品编号、名称和已保存的产品定义版本；切换产品后自动刷新该产品的缺陷类别、图片、标注和数据集版本；数据库 Schema 升级至 V5，在数据集图片、数据集版本和版本图片快照中持久化 `ProductDefinitionVersion`，并为既有数据执行兼容回填。
- 验证结果：使用两个独立产品分别保存 `PD-1.2.3` 和 `PD-2.0.0`，验证了图片、类别、标注、数据集版本和版本快照完全隔离；同一标注页面切换当前产品后能够即时刷新；Debug、Release 和 Visual Studio 设计时构建均为 0 警告、0 错误。
- 关联提交：本次产品与标注关联修复提交。
- 遗留事项：当前产品选择在单次应用会话内保持；如需跨应用重启恢复最后选择，可后续增加项目级偏好持久化。

### 2026-08-08 18:06 — 数据集图片导入与安全删除完善

- 开发目标：让数据集标注页面具备适合日常使用的图片路径导入、批量导入和图片删除闭环。
- 完成内容：将导入入口细分为多文件导入和文件夹导入，支持递归扫描子文件夹、拖拽文件或文件夹、同批路径去重、格式过滤以及失败明细统计；图片列表支持 `Ctrl/Shift` 多选、工具栏删除和 `Delete` 快捷键；删除前展示图片与关联标注数量并二次确认，删除时校验当前产品归属，在事务内清理图片记录及其全部标注，同时清理未发布图片的 Workspace 副本；若图片已被历史数据集版本引用，则只从当前数据集移除并保留版本所需文件，确保已发布版本不被破坏。
- 验证结果：Debug 构建通过；隔离 Workspace 冒烟测试验证了文件夹递归扫描、重复路径去重、图片导入、标注级联删除、未发布文件清理、已发布版本快照和文件保留，以及跨产品删除拦截。
- 关联提交：本次数据集图片导入与删除实用功能提交。
- 遗留事项：超大规模数据集后续可增加可取消的后台导入进度和基于文件内容哈希的跨批次重复图片检测。

### 2026-08-08 18:20 — 修复产品类别与标注类别不一致

- 开发目标：确保产品定义页面编辑的瑕疵类别可靠落库，并让数据标注始终使用当前产品的最新类别。
- 完成内容：定位到类别表格修改后只有点击“保存选中”才会写入数据库的问题；改为类别单元格值提交后立即自动保存，并将按钮文案明确为“保存当前类别”；顶部“保存产品定义”增加保存全部类别的兜底逻辑和产品归属校验；类别区域显示自动保存状态及当前类别数量；实时标注查询和数据集版本快照按类别 ID 关联 `DefectCategories`，优先使用最新类别编码与名称，类别删除后仍保留历史标注快照名称。
- 验证结果：读取当前 Debug Workspace 确认产品最后保存时间晚于类别记录且数据库仍为默认“新缺陷1”，与未点击独立类别保存按钮的现象一致；WinForms 隔离回归测试验证了单元格自动保存、全部类别兜底保存、已有标注名称同步、版本快照名称同步以及双产品类别隔离；Debug、Release 和 Visual Studio 设计时编译均通过。
- 关联提交：本次产品类别与标注类别同步修复提交。
- 遗留事项：此前仅停留在界面、从未写入数据库的类别文本无法自动恢复，升级后需要重新输入一次；之后每次编辑都会自动保存。

### 2026-08-08 18:47 — 瑕疵模板识别基础闭环

- 开发目标：将“瑕疵模板识别”页面从静态演示数据升级为可基于当前产品、类别和数据集实际运行的少样本辅助标注功能。
- 完成内容：数据库 Schema 升级至 V6，新增按产品/类别隔离的识别参数、候选批次和 Hard Negative 持久化；以数据集中已确认的类别标注作为正样本，使用纯 CPU 灰度特征、类别原型向量、三尺度滑窗、相似度阈值、Top-K、已有标注排除、候选池限制和非极大值抑制生成候选；Hard Negative 原型会对相似候选降分；支持候选图与相似度区域预览、边缘框精修、人工拒绝、加入难负样本，以及人工确认后直接写回真实数据集标注；页面会跟随当前产品切换，类别、正样本、负样本、候选和本轮统计全部读取真实数据，并对无产品、无类别、无正样本和无候选状态给出操作提示；调整顶部参数栏、右侧操作区和学习循环区的 DPI 自适应布局。
- 验证结果：合成纹理端到端测试验证了相同瑕疵区域命中、候选确认写回标注、正样本增长、边缘框精修、拒绝、Hard Negative、预览和真实页面绑定；旧 Schema V5 数据库副本可无损升级到 V6；页面渲染检查通过；Debug、Release 和 Visual Studio 设计时编译均通过且无错误。
- 关联提交：本次瑕疵模板识别基础闭环提交。
- 遗留事项：当前匹配器是无需外部运行时的 CPU 基础实现，适合作为业务闭环和算法接口基线；ONNX/CUDA/TensorRT 特征提取、SAM2 像素级 Mask 精修、HALCON 模板算子、可取消的批量进度和工业现场阈值标定仍需后续接入与验收。

### 2026-08-08 19:08 — README 项目说明同步

- 开发目标：使 GitHub 仓库首页准确反映当前实现状态，并为首次使用者提供清晰的运行和操作指引。
- 完成内容：重写 `README.md`，补充产品定义、数据集标注、产品联动和瑕疵模板识别的完成状态；增加推荐业务流程、数据集常用操作、技术栈、x64 构建步骤、Workspace 数据目录、代码结构、算法能力边界和后续集成方向。
- 验证结果：README 中的框架版本、平台配置、依赖、运行时路径和业务能力均已与当前工程及最新实现核对；Markdown 结构检查通过。
- 关联提交：本次 README 更新提交。
- 遗留事项：后续训练、在线检测或外部运行时完成接入时，需要同步更新仓库首页的模块状态与部署说明。

### 2026-08-10 09:15 — 数据集已有标注编辑、撤销重做与画布导航

- 开发目标：补齐数据集标注页面在精细人工修正阶段缺少已有标注编辑、Undo/Redo 和局部放大浏览的问题，提高实际标注效率。
- 完成内容：将原运行时“Mask 编辑”入口调整为“编辑标注”；新增已有标注选择和高亮，支持 Rectangle / Polygon / Brush 整体拖动，Rectangle 提供四角与四边共 8 个控制点缩放，Polygon 支持顶点拖动；所有几何修改通过独立 `AnnotationEditingService` 校验后写回 SQLite；新增当前图片会话级操作历史，`Ctrl+Z` / `Ctrl+Y` 支持新增、删除、移动、矩形缩放和 Polygon 顶点编辑的撤销/重做，最多保留 100 步且切换图片后清空；画布加入鼠标滚轮以光标位置为中心的 25%~1200% 缩放、中键拖动或 `Space+左键` 平移、双击编辑画布及 `Home/0` 恢复 Fit，并限制平移范围避免图片完全移出视野；画布标题同步显示当前缩放比例和快捷操作说明。
- 验证结果：首次 CI 定位并修复数据标注 Canvas 缺少主题引用导致的编译错误；修复后 GitHub Actions `WinForms Build` 的 NuGet Restore 与 Debug x64 Rebuild 均通过。当前连接环境无法代替本机进行真实鼠标拖动手感和高 DPI 交互验收，仍需在 Visual Studio 本地运行后按矩形缩放、Polygon 顶点拖动、连续 Undo/Redo、滚轮缩放和平移逐项人工确认。
- 关联提交：PR #8（本次数据集标注交互完善提交）。
- 遗留事项：Undo/Redo 为当前图片、当前运行会话的内存历史，不跨图片和应用重启持久化；Brush 当前只支持整体移动，不提供轨迹节点级编辑；真正的像素级 Mask 编辑、Mask PNG 输出、COCO/YOLO 导入导出和大数据集后台任务仍待后续实现。
